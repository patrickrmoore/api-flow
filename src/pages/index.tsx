import React from "react";
import { AppShell } from "../components/app-shell";
import { Grid } from "./grid";
export const App = () => {
  return (
    <AppShell>
      <Grid />
    </AppShell>
  );
};
